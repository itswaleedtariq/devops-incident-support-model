# ============================================================
# DevOps Incident Support Model — Backend Dependencies
# ============================================================

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Data validation & settings
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Environment loading
python-dotenv>=1.0.0

# ============================================================
# Database
# ============================================================
# SQLAlchemy with async extras (installs greenlet automatically)
sqlalchemy[asyncio]>=2.0.36
# PostgreSQL async driver
asyncpg>=0.30.0
# Database migrations
alembic>=1.14.0

# ============================================================
# Security & Authentication
# ============================================================
# Password hashing (bcrypt-based)
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0,<5.0
# JWT (access + refresh tokens)
PyJWT>=2.9.0
# Email validation for Pydantic EmailStr
email-validator>=2.2.0
# Rate limiting
slowapi>=0.1.9

# ============================================================
# Testing
# ============================================================
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# Test coverage
pytest-cov>=6.0.0
